/** @jsxImportSource @emotion/react */
const StyledContainer = ({ children }) => (
  <div css={{
    display: "flex",
    maxWidth: "1440px",
    margin: "auto"
  }}>{ children }</div>
)

export default StyledContainer;
